<%# locals: team %>
<li class="home-card flex flex-col gap-3 px-6 py-4 sm:flex-row sm:items-center sm:justify-between">
  <div class="min-w-0">
    <%= link_to team.name, facilitator_team_path(team), class: "font-semibold text-ink hover:text-coral" %>
    <p class="mt-2 text-sm leading-6 text-[#466057]">No active retrospective</p>
  </div>
  <%= link_to "Create retrospective", new_facilitator_team_retrospective_path(team), class: home_primary_button_classes %>
</li>
