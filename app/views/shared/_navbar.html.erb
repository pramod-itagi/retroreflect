<header class="app-header">
  <div class="app-header-inner">
    <%= link_to root_path, class: "app-brand" do %>
      <%= render "shared/brand_mark" %>
      <span class="app-brand-name">Retroreflect</span>
    <% end %>

    <% if authenticated? %>
      <nav class="app-nav" aria-label="Main">
        <%= nav_link "Home", root_path %>
        <%= nav_link "Teams", facilitator_teams_path %>
        <%= nav_link "Retrospectives", retrospectives_path %>
        <%= nav_link "Action items", participant_action_items_path %>
        <% if current_user.system_admin? %>
          <%= nav_link "System administration",
                       system_admin_root_path,
                       class: "app-nav-admin",
                       aria: { label: "System administration" } do %>
            <span class="app-nav-admin-full">System administration</span>
            <span class="app-nav-admin-short" aria-hidden="true">Admin</span>
          <% end %>
        <% end %>
      </nav>

      <%= render "shared/account_menu" %>
    <% else %>
      <nav class="app-nav app-nav-auth" aria-label="Account">
        <%= nav_link "Sign in", new_session_path %>
        <%= link_to "Register", new_registration_path, class: "app-header-register" %>
      </nav>
    <% end %>
  </div>
</header>
