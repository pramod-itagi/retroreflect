<div class="workspace-page workspace-shell listing-page error-page">
  <div class="listing-inner">
    <header class="listing-intro">
      <p class="listing-kicker"><%= code %></p>
      <h1 class="display-face listing-heading"><%= heading %></h1>
      <p class="listing-lede"><%= lede %></p>
      <div class="error-actions">
        <%= yield %>
      </div>
    </header>
  </div>
</div>
