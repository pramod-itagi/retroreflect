<% content_for :full_bleed, true %>
<% content_for :head do %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,600;9..144,700&family=Work+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
<% end %>

<div class="workspace-page workspace-shell listing-page admin-page">
  <div class="listing-inner">
    <header class="listing-intro">
      <p class="listing-back">
        <%= back_nav_link system_admin_root_path, "system administration" %>
      </p>
      <p class="listing-kicker">Administration</p>
      <h1 class="display-face listing-heading">System Admins</h1>
      <p class="listing-lede">People who can create teams and manage administrators.</p>
    </header>

    <div class="listing-stack">
      <ul class="home-section-list">
        <% @system_admins.each do |admin| %>
          <li>
            <article class="home-card admin-row flex flex-col gap-3 px-6 py-4 sm:flex-row sm:items-center sm:justify-between">
              <p class="min-w-0 text-ink">
                <span class="font-semibold"><%= admin.display_name %></span>
                <span class="text-[#466057]"> · <%= admin.email %></span>
              </p>
              <% if admin.last_system_admin? %>
                <p class="admin-constraint shrink-0">At least one System Admin must remain.</p>
              <% else %>
                <%= button_to "Remove",
                              system_admin_admin_path(admin),
                              method: :delete,
                              class: "text-sm font-semibold text-[#466057] hover:text-coral",
                              form_class: "shrink-0" %>
              <% end %>
            </article>
          </li>
        <% end %>
      </ul>

      <% if @candidate_users.any? %>
        <%= form_with url: system_admin_admins_path, class: "home-card mt-8 grid gap-4 px-6 py-5 sm:grid-cols-[minmax(0,1fr)_auto] sm:items-end" do %>
          <div class="flex min-w-0 flex-col gap-1.5">
            <%= label_tag :user_id, "Add System Admin", class: "text-xs font-semibold uppercase tracking-[0.12em] text-[#466057]" %>
            <%= select_tag :user_id,
                           options_from_collection_for_select(@candidate_users, :id, :display_name),
                           prompt: "Select a confirmed user",
                           class: "workspace-filter-field pr-10" %>
          </div>
          <%= submit_tag "Add", class: "#{home_primary_button_classes} w-full cursor-pointer sm:w-auto" %>
        <% end %>
      <% else %>
        <p class="mt-8 text-sm leading-6 text-[#466057]">Everyone with a confirmed account is already a System Admin.</p>
      <% end %>
    </div>
  </div>
</div>
